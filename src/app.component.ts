import {Component} from '@angular/core';

@Component({
  selector: 'app-root',
  template: `
  <div class="container">
    <div class="jumbotron">
      <h1>zoo-database</h1>
    </div>
  </div>
  `
})

export class AppComponent {
  // Component logic
}
